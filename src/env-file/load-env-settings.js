//  load-env-file.js
//
//  Exports a function which loads a given env from an env file.

function loadEnvFile(envFile, env) {
  const contents = loadContents(envFile);

  //  check the env is present

  //  check the env is structured properly
}

module.exports = loadEnvFile;

